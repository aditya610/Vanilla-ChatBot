'use strict';

let message = "";

message = `You Can :\n`
message += `#1 Ask for a joke \n`
message += `\t(cmd : joke) \n\n`
message += `#2 Ask for weather conditions \n`
message += `\t(for eg: whats the weather like in New York) \n\n`
message += `#3 If you are feeling lonely we can simply talk \n`
message += `\t(for eg : hi , wassup?)`

module.exports = message;
